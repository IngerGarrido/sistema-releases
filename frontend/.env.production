# Producción: ruta relativa para que el build funcione en cualquier dominio
# sin necesidad de recompilar. Frontend y backend se sirven del mismo origen.
VITE_API_URL=/backend/api

VITE_APP_ENV=production
